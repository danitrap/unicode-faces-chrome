# Unicode Faces (Chrome Extension)

This extension allows you to copy cool unicode faces.

## Installation

Fork this repo and run `bower install` to install its dependencies, then load as an unpacked extension into Chrome.
